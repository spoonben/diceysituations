Dicey Situations Podcast Website
===

To run you'll need to add `config.js` to the `src/` directory:

```
const Config = {
    audio_files_location: '[YOUR-AUDIO-FILES-LOCATION]',
}

export default Config;
```
